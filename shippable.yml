language: php

php:
  - 5.6
env:
  global:
    - FORTRABBIT_HOST=deploy.eu2.frbit.com
    - DEV_REPO=dev-forms.git
    - PROD_REPO=prod-forms.git
    - APP_ENV=test
    - SYMFONY__DATABASE__HOST=127.0.0.1
    - SYMFONY__DATABASE__PORT=~
    - SYMFONY__DATABASE__NAME=dbname
    - SYMFONY__DATABASE__USER=shippable
    - SYMFONY__DATABASE__PASSWORD=
    - SYMFONY__CSS=dev
    
before_script:
  - echo $FORTRABBIT_HOST
  - echo $DEV_REPO
  - echo $PROD_REPO
  - echo $APP_ENV
  - echo $SYMFONY__DATABASE__HOST
  - echo $SYMFONY__DATABASE__PORT
  - echo $SYMFONY__DATABASE__NAME
  - echo $SYMFONY__DATABASE__USER
  - echo $SYMFONY__CSS

script:
  - echo $FORTRABBIT_HOST
  - echo $DEV_REPO
  - echo $PROD_REPO
  - echo $APP_ENV
  - echo $SYMFONY__DATABASE__HOST
  - echo $SYMFONY__DATABASE__PORT
  - echo $SYMFONY__DATABASE__NAME
  - echo $SYMFONY__DATABASE__USER
  - echo $SYMFONY__CSS


after_script:
  - echo $FORTRABBIT_HOST
  - echo $DEV_REPO
  - echo $PROD_REPO
  - echo $APP_ENV
  - echo $SYMFONY__DATABASE__HOST
  - echo $SYMFONY__DATABASE__PORT
  - echo $SYMFONY__DATABASE__NAME
  - echo $SYMFONY__DATABASE__USER
  - echo $SYMFONY__CSS
